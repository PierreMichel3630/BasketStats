import { getDocument } from "pdfjs-dist";

export const readPDF = async (url: string) => {
  /*const loadingTask = getDocument(url);
  loadingTask.promise.then((pdf) => {
    console.log(pdf);
  });*/
};
